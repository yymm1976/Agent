{
  "id": "danger-cmd-confirm",
  "name": "危险命令确认",
  "description": "执行 shell 命令前检测是否包含 rm -rf、git push --force 等危险操作",
  "matchKeywords": ["危险命令", "danger", "确认", "confirm", "rm -rf", "force push"],
  "event": "pre-tool-call",
  "condition": { "toolName": "shell_exec" },
  "command": "echo '请确认是否执行此命令'",
  "failBehavior": "block"
}
